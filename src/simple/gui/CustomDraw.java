package simple.gui;

/** Interface intended to be used as a way to customize how drawing is done on widgets. You can see an example of its usage in the button class. **/
public interface CustomDraw {
	public void draw(Widget w);
}
